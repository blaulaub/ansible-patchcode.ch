# Autogenerated by ansible. Any changes are likely to be overwritten
# the next time ansible runs.

127.0.0.1	localhost

{% for network in network_mappings.managed_networks %}
{% for item in network_mappings[network].hosts %}
{{ item.ip }} {{ item.host }}.{{ network_mappings[network].domain }} {{ item.host }} 
{% endfor %}
{% endfor %}

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
