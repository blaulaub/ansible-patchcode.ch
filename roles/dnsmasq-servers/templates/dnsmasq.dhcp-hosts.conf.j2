# Static dhcp records for dnsmasq.
#
# Autogenerated by ansible. Any changes are likely to be overwritten
# the next time ansible runs.

{% for item in network_mappings['localnet'].hosts %}
dhcp-host={{ item.mac }},{{ item.host }},{{ item.ip }},2h
{% endfor %}
