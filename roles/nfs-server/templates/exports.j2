# /etc/exports: the access control list for filesystems which may be exported
#               to NFS clients.  See exports(5).
#
# Example for NFSv2 and NFSv3:
# /srv/homes       hostname1(rw,sync,no_subtree_check) hostname2(ro,sync,no_subtree_check)
#
# Example for NFSv4:
# /srv/nfs4        gss/krb5i(rw,sync,fsid=0,crossmnt,no_subtree_check)
# /srv/nfs4/homes  gss/krb5i(rw,sync,no_subtree_check)
#
# ---
# Autogenerated by ansible. Any changes are likely to be overwritten
# the next time ansible runs.

{% for item in nfs_exports %}
{{ item.heading|comment }}
{% for path in item.paths %}
{{ "%-32s"|format(path.path) }} {% for rw in path.rw_clients %} {{ rw }}(rw,sync,no_subtree_check,no_root_squash){% endfor %}{% for ro in path.ro_clients %} {{ ro }}(ro,sync,no_subtree_check,no_root_squash){% endfor %}

{% endfor %}

{% endfor %}
